# 1.在内核态出现中断嵌套时应该怎么处理
- 首先结论：linux是不支持中断嵌套

- **中断上半部**

- 在linux驱动开发中，内核将会自行处理中断嵌套的问题，作为开发者一般只需要遵循Linux规定的框架，上半部分尽可能的简短，任务推向下半部处理。

- Linux在处理中断时，会禁用本地CPU中断，防止出现同一个CPU中断嵌套的现象。如果是多核系统，那么新中断会路由到空闲CPU，确保同一CPU同一时间只处理一个中断，避免中断嵌套的现象。

-       如果出现新中断，但没有空闲CPU。那么中断控制器将会管理状态机：
           - 当没有中断时，中断控制器处于Inactive状态。
        
           - 当出现一个中断时，那么将会进入pending状态等待CPU处理。
        
           - 当该中断被某个CPU选中并执行，那么此时进入Active状态。
        
           - 当处理第一个中断时，又出现了第二个中断。GIC中断控制器会将第二个中断设置为Active and pending。
        
           - 此时等待第一个中断完成，第二个中断才会执行
        
           - 虽然Linux不支持中断嵌套，但是在GIC中断控制器中存在抢占机制。在CPU处理第一个中断时，出现了两个中断，那么优先级高的中断将在CPU处理完第一个中断后优先执行。（类似于CAN仲裁机制）
        
             ![image-20251125145230180](.\image\image-20251125145230180.png)
        
- 下图是arm官方手册，对于中断处理嵌套处理机制时序图，感兴趣的自行分析。

  ![image-20251125145446048](.\image\image-20251125145446048.png)
-	**上半部与下半部**
-		如上情况都是对于中断上半部的情况。存在一种情况，当第一个中断处于中断下半部的时候，此时来了一个相同在中断的情况。那么此时的中断下半部是可以被打断的。
-		因此可能存在下半部和上半部访问**共享数据**的情况，因此需要用spin_lock_irqsave和spin_unlock_irqstore同步保护，**该锁会禁用中断，防止临界区出现新中断也获取该锁，导致死锁**。不过这样会导致新中断调用延迟，因此需要确保临界区足够的短。
-		下半部在执行，出现一个相同的中断，而在中断上半部没有确认是否有下半部正在运行，那么将会出现**下半部被反复调用**，因此需要使用原子变量确认是否有下半部正在运行。
-		中断上半部和下半部是多对一的关系。因此存在**下半部丢失**的情况，需要考虑丢失现象是否能够接受，如果不能接受可尝试利用某个变量记录上半段次数来确保下半部无丢失现象。
-		除此之外，可能还存在下半部被长时间打断，导致延迟严重。如果下半部中存在一些需要响应的操作，会**导致超时现象**。
-		除了上面情况，可能还存在新中断导致中断被分发到其他CPU导致一些本地内存问题。（这些东西我还没遇到过，还不太懂，查了一些资料，术语太多了，需要进一步去查看）